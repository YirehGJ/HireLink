# Build Project

Follow these steps to install dependencies and build the Next.js project:

1. Install dependencies:
   ```bash
   npm install
   ```
2. Build the project:
   ```bash
   npm run build
   ```
